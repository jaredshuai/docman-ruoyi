spring:
  config:
    activate:
      on-profile: local
  datasource:
    dynamic:
      datasource:
        master:
          url: jdbc:mysql://localhost:3307/ry-vue?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&serverTimezone=GMT%2B8&autoReconnect=true&rewriteBatchedStatements=true&allowPublicKeyRetrieval=true&nullCatalogMeansCurrent=true
          username: root
          password: root

spring.data:
  redis:
    host: localhost
    port: 6380
    database: 0
    password: ruoyi123
    timeout: 10s
    ssl.enabled: false

snail-job:
  # 本地验证默认关闭 SnailJob，但 ruoyi-job 示例 Bean 仍会读取该占位符
  port: 0

docman:
  viewer:
    enabled: true
    ticket-ttl-seconds: 300
    base-url: http://localhost:8012
